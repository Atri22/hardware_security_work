# hardware_security_work
For Profiled Correlation Attack first run capture_trace_pca.py to capture_profile_trace.py. It uses pt_.txt and key_.txt. Then run pca-attack.py it uses pt_attck.txt and fixed_key.txt. 
For Standerd CPA run the standerd_cpa.py file, it uses fixed_key.txt and pt_attck.txt.
